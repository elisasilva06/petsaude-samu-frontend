import { cadastroMockService } from './cadastro.mock.service';
import type { CadastroService } from './cadastro.service';

/**
 * TODO(BACKEND):
 * Trocar apenas a implementação abaixo por
 * cadastroApiService quando a API estiver disponível.
 *
 * As telas não precisarão ser alteradas.
 */
export const cadastroService: CadastroService =
  cadastroMockService;